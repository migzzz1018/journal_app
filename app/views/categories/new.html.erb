<%= form_with model: @category, local: true do |f| %>

    <h1>Create new Category</h1>

    <p>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.submit %>
        
    </p>

    <p><%= link_to 'back', :back %></p>

<% end %>